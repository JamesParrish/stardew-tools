import { Times } from './Times';
import { Location } from './Location';

export class Fish {
    name: string;
    id: Number;
    difficulty: Number;
    times: Times[];
    weather: string;
    bundle: string;
    locations: Location[];
}